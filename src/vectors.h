#pragma once

struct Vector2 {
    int x;
    int y;
};

struct Vector3 {
    int x;
    int y;
    int z;
};

// math operations and such can be added in the future, the structs are fine for now though
